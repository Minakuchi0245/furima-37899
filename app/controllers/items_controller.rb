class ItemsController < ApplicationController
  def index
  end

  #private
  #def item_params
  #  params.require(:item).permit(:item_instruction, :category_id, :condition_id, :shipping_charge_id, :shipping_area_id, :shipping_days_id, :price , :user_id, :image).merge(user_id: current_user.id)
  #end


end
